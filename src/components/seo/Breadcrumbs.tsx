import Link from "next/link";export function Breadcrumbs({items}:{items:{label:string;href?:string}[]}){return <nav aria-label="Breadcrumb" className="flex flex-wrap gap-2 font-mono text-[10px] uppercase tracking-[.15em] text-obsidian/45">{items.map((i,n)=><span className="flex gap-2" key={i.label}>{n>0&&<span>/</span>}{i.href?<Link href={i.href} className="hover:text-blue">{i.label}</Link>:<span>{i.label}</span>}</span>)}</nav>}
