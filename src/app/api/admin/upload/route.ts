import { NextResponse } from "next/server";import { requireAdmin } from "@/lib/auth";
export async function POST() {try{await requireAdmin();const cloud=process.env.CLOUDINARY_CLOUD_NAME,key=process.env.CLOUDINARY_API_KEY,secret=process.env.CLOUDINARY_API_SECRET;if(!cloud||!key||!secret)return NextResponse.json({error:"Configure Cloudinary environment variables for production image uploads."},{status:501});return NextResponse.json({error:"Cloudinary upload signing should be configured with your storage policy before production."},{status:501})}catch{return NextResponse.json({error:"Unauthorized"},{status:401})}}
